# CPP08
Templated containers, iterators, algorithms
	> STL(Standard Template Library)
> Ex00: Find Element in container using STL algorithms
> Ex01: Store Numbers and find shortes/longest span
> Ex02: MutantStack - Make std::stack iterable

### What's an iterator?
	> it is like a pointer that let us tranvers in container
### What's a container?
	> Is a template class used to store a collection of objects
*	So std::find what it does
	> it takes 2 iterator(.begin, .end) and a value to search for
	> return an iterator if found elemenet or last if not found

